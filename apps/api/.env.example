# PostgreSQL connection used by Prisma and the Nest API
DATABASE_URL=postgresql://opsflow:opsflow@localhost:5432/opsflow?schema=public

# Clerk backend secret used to verify API and realtime tokens
CLERK_SECRET_KEY=sk_test_replace_me

# Optional API port
PORT=4000
